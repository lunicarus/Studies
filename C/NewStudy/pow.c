#include <math.h>
#include <stdio.h>


//!power functions will be made in Online IDE's
int main()
{
    double x = 6.1, y = 4.8;
 
    // Storing the answer in result.
    double result = pow(x, y);
    printf("%.2lf", result);
 
    return 0;
}
#include <math.h>
#include <stdio.h>